{
  "name": "@repo/cloudflare-peerjs-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Durable Objects-based PeerJS signaling server",
  "scripts": {
    "dev": "wrangler dev --port 9000",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.11.0",
    "eslint": "^9.34.0",
    "miniflare": "^3.20231218.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0",
    "wrangler": "^3.20.0"
  }
}
