# ============================================================================
# Discord Gateway Real-Time Event System - Environment Variables
# ============================================================================

# Discord Bot Token (server-side only, NEVER expose to browser)
# Required for Discord Gateway connection and API calls
DISCORD_BOT_TOKEN=your-discord-bot-token-here

# Discord Guild ID (can be in client - already in VITE_DISCORD_GUILD_ID)
# Used for voice channel operations
VITE_DISCORD_GUILD_ID=your-guild-id-here

# HMAC Secret for Gateway→Web Communication (server-side only, NEVER expose to browser)
# Used to sign and verify events from Discord Gateway Worker
# Minimum 32 characters recommended
HUB_SECRET=development-hub-secret-change-me-minimum-32-chars

# Room Token Secret (server-side only)
# Used to sign and verify invite tokens for private voice rooms
# Minimum 32 characters recommended
ROOM_TOKEN_SECRET=development-room-secret-change-me-minimum-32-chars

# Base URL for Hub Endpoint (used by Discord Gateway to post events)
# Defaults to http://localhost:1234 for development
VITE_BASE_URL=http://localhost:1234

# ============================================================================
# Notes:
# - All secrets (DISCORD_BOT_TOKEN, HUB_SECRET, ROOM_TOKEN_SECRET) must be
#   server-side only and NEVER exposed to the browser
# - VITE_DISCORD_GUILD_ID and VITE_BASE_URL are safe for client code
# - Use strong, random values for production (minimum 32 characters)
# ============================================================================