VITE_EMBEDDINGS_VERSION=latest-dev

VITE_SUPPORT_URL=https://ko-fi.com/spell_coven

# Use dev deployment (must have E2E_TEST=1 and PREVIEW_LOGIN_CODE in Convex Dashboard).
# For preview-based e2e, run `bun run convex:e2e:ui` from repo root instead.
VITE_CONVEX_URL=https://elated-bat-257.convex.cloud